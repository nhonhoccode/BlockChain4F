# Cho phép sử dụng mock data cho OAuth trong môi trường phát triển
ALLOW_MOCK_OAUTH = True

# Đường dẫn redirect mặc định cho Google OAuth
GOOGLE_REDIRECT_URI = 'http://localhost:3000/auth/google/callback' 